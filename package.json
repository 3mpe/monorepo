{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "clean": "npx lerna clean -y",
    "bootstrap": "lerna bootstrap --hoist",
    "rebuild": "npm run bootstrap --hoist && npm run clean && npm run start"
  }
}
